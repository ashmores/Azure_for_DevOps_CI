trigger:
- main
- releases/*

pool:
  name: New Agent Pool
  demands:
   - agent.name -equals Build Agent A

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script v3'

- script: |
    echo Add a multiline script v3
    echo One more v3
  displayName: 'Run a multi-line script v3'
